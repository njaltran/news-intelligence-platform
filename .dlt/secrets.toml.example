# Copy to `.dlt/secrets.toml` and fill in. The real file is gitignored.

[sources.newsapi]
api_key = "your-newsapi-key"

# Local docker ClickHouse (matches infra/docker-compose.yml).
[destination.clickhouse.credentials]
password = "news"
