# data/interim

Cleaned and dedup'd data produced by the pipeline. Generally **gitignored** because it is rebuildable from `data/raw/` via the dlt pipeline.

Sample outputs (for quick reference and tests) may live here, but the canonical artifacts live in the DuckDB raw lake and the ClickHouse warehouse.
